"""Entry point — wire the pieces together and talk to the agent.

TODO:
  - import Agent from agent.py
  - import your tool objects from tools/example_tools.py
  - construct an Agent(model="llama3.1", tools=[...], system_prompt="...")
  - loop: read a line from input(), call agent.run(line), print the result
    (Ctrl+C / an "exit" check to break out)
"""

if __name__ == "__main__":
    raise NotImplementedError
